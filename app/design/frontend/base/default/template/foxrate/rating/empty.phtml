<?php
/**
 * If Shop has no orders or/and API has no imported products, we have no products to show, so
 * API has nothing to show even on new product form.
 */

if (isset($foxrateFiDebugMessage)) {
    echo $foxrateFiDebugMessage->getFormatedCode();
}
